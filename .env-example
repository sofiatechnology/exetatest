# General Settings

ADMIN_SECRET=11bdfs455-b2bf-40c1-a7d4-4068b461d6d5
BASE_URL=http://localhost:3000

# DATABASES

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=1234
DB_NAME=exetatest

# Frontend URL
FRONTEND_URL=http://localhost:4200

# Email Configuration - Choose one provider: resend or smtp
# Render deployment should use Resend.
EMAIL_PROVIDER=resend

# Resend Configuration
RESEND_API_KEY=re_xxxxxxxxxxxxx
FROM_EMAIL=noreply@yourdomain.com

# SMTP Configuration (Nodemailer fallback, deprecated)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=user@gmail.com
SMTP_PASS=faoo hpkg lkow grnk

# Email From Address (legacy fallback)
EMAIL_FROM=noreply@user.com
SMTP_FROM=noreply@user.com

# SCRIPT COMMAND


# deployment

```bash
ADMIN_SECRET="your-admin-secret" BASE_URL="https://your-app.example.com" ./make-admin.sh user@example.com
```

# link to generate app password

https://myaccount.google.com/apppasswords
