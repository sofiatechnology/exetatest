# Email Provider

Use these variables on Render to send email through Resend:

```env
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxxxxxxxxxxxx
FROM_EMAIL=noreply@yourdomain.com
```

`EMAIL_PROVIDER` defaults to `resend` when it is not set.
